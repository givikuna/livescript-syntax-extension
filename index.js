// Generated by LiveScript 1.6.0
(function(){
  var lib, fs, ref$, floor, filter, map, sort, unique, empty;
  lib = require('./lib');
  fs = require('fs');
  ref$ = require('prelude-ls'), floor = ref$.floor, filter = ref$.filter, map = ref$.map, sort = ref$.sort, unique = ref$.unique, empty = ref$.empty;
  function lispify(){
    var i$, ref$, len$, key, results$ = [];
    for (i$ = 0, len$ = (ref$ = Object.keys(lib)).length; i$ < len$; ++i$) {
      key = ref$[i$];
      results$.push(global[key] = lib[key]);
    }
    return results$;
  }
  module.exports = {
    lispify: lispify,
    bool: lib.bool,
    dir: lib.dir,
    currentDir: lib.currentDir,
    print: lib.print,
    equals: lib.equals,
    equalsAny: lib.equalsAny,
    isNotBlank: lib.isNotBlank,
    isBlank: lib.isBlank,
    input: lib.input,
    readFile: lib.readFile,
    readDir: lib.readDir,
    isFile: lib.isFile,
    isDir: lib.isDir,
    exists: lib.exists,
    readDirs: lib.readDirs,
    readFiles: lib.readFiles,
    countDirs: lib.countDirs,
    countFiles: lib.countFiles,
    makeLegalJavaScriptName: lib.makeLegalJavaScriptName,
    defun: lib.defun,
    define: lib.define,
    defmacro: lib.defmacro,
    defconstant: lib.defconstant,
    defparameter: lib.defparameter,
    defvar: lib.defvar,
    lambda: lib.lambda,
    fn: lib.fn,
    clone: lib.clone,
    echo: lib.echo,
    execute: lib.execute,
    exec: lib.exec,
    println: lib.println,
    funcall: lib.funcall,
    def: lib.def,
    length: lib.length,
    sleep: lib.sleep,
    lazy: lib.lazy,
    throttle: lib.throttle,
    debounce: lib.debounce,
    noop: lib.noop,
    identity: lib.identity,
    before: lib.before,
    after: lib.after,
    say: lib.say,
    len: lib.len,
    isNumeric: lib.isNumeric,
    lower: lib.lower,
    upper: lib.upper,
    supertrim: lib.supertrim,
    stringToNumber: lib.stringToNumber,
    booleanToString: lib.booleanToString,
    str: lib.str,
    flatStr: lib.flatStr,
    strToTitle: lib.strToTitle,
    rev: lib.rev,
    getFileExtension: lib.getFileExtension,
    int: lib.int,
    random: lib.random,
    roundToXDigits: lib.roundToXDigits,
    toDecimal: lib.toDecimal,
    toBinary: lib.toBinary,
    toHex: lib.toHex,
    positive: lib.positive,
    inc: lib.inc,
    dec: lib.dec,
    ONCE: lib.ONCE,
    nothing: lib.nothing,
    t: lib.t,
    nil: lib.nil,
    f: lib.f,
    occurs: lib.occurs,
    occursIn: lib.occursIn,
    occurrencesOf: lib.occurrencesOf,
    countOccurrencesIn: lib.countOccurrencesIn,
    isJSON: lib.isJSON,
    isArray: lib.isArray,
    transpose: lib.transpose,
    chunk: lib.chunk,
    list: lib.list,
    mapcar: lib.mapcar,
    append: lib.append,
    uniq: lib.uniq,
    sorted: lib.sorted
  };
}).call(this);
